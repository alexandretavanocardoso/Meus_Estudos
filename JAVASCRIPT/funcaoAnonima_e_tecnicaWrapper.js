// Funcoes anonimas, não possuem nome

function (params) {
    document.write("olá");
};

// Wrapper
// associar uma função pra variavel

var exibir = function (params) {
    document.write("olá");
};

exibir();